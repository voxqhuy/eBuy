@model List<eBuy.Models.Product>

@{
    ViewBag.Title = "eBuy";
}

<h2>Index</h2>

<div class="row">
    <div class="col-md-9">
        <div>
            <button type="button" id="showAll" class="btn btn-sm btn-warning">Show all</button>
        </div>

        <ul id="productList" class="list-group mb-3">
            @*@foreach (var item in Model)
            {
                <li class="list-group-item">
                    <div class="row" style="height:90px">
                        <div class="col-md-8">
                            <h6 class="my-0">
                                @Html.DisplayFor(modelItem => item.product_name)
                            </h6>
                            <small class="text-muted">
                                @Html.DisplayFor(modelItem => item.product_description)
                            </small>
                        </div>
                        
                        <div class="text-muted col-md-2">
                            $@Html.DisplayFor(modelItem => item.price)
                        </div>
                        <div class="col-md-2" style="height:100%">
                            <button type="button" class="btn btn-block btn-primary" style="height:100%">Add to cart</button>
                        </div>
                    </div>
                    
                </li>
            }*@
            
        </ul>
    </div>
    <div class="col-md-3">
        Users
        <h4 class="d-flex justify-content-between align-items-center mb-3">
            <span class="text-muted">Your cart</span>
        </h4>
        <ul class="list-group mb-3">
            <li class="list-group-item">
                <div>
                    <h6 class="my-0">Product name</h6>
                    <small class="text-muted">Brief description</small>
                </div>
                <span class="text-muted">$12</span>
            </li>
            <li class="list-group-item">
                <div>
                    <h6 class="my-0">Second product</h6>
                    <small class="text-muted">Brief description</small>
                </div>
                <span class="text-muted">$8</span>
            </li>
            <li class="list-group-item">
                <div>
                    <h6 class="my-0">Third item</h6>
                    <small class="text-muted">Brief description</small>
                </div>
                <span class="text-muted">$5</span>
            </li>
            <li class="list-group-item">
                <span>Total (USD)</span>
                <strong>$20</strong>
            </li>
        </ul>
    </div>
</div>

<script src="/Scripts/jquery-1.10.2.js" type="text/javascript"></script>
<script type="text/javascript">
    $(document).ready(function () {
        var showAllUrl = '@Url.Action("showAllProducts", "Home")';
        // show all products when "Show All" is pressed
        $('#showAll').click(function() {
            $('#productList').load(showAllUrl);
        })
    })
</script>